import { describe, beforeAll, afterAll, it, expect } from "@jest/globals";
import { donateOrder } from "../src/api/order/services/donateOrder";

describe("donateOrder", () => {
  it("should donate an order", async () => {});
});
